{
    "name": "smart-study-planner-root",
    "version": "1.0.0",
    "description": "Root package for deployment orchestration",
    "scripts": {
        "install-client": "cd client && npm install",
        "install-server": "cd server && npm install",
        "build": "npm run install-client && npm run install-server && cd client && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/* server/public/",
        "start": "cd server && node server.js"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "engines": {
        "node": ">=14.0.0"
    }
}